# Trigger workflows on pull request review events
name: ON-PR-MERGE
run-name: PR merge to Main for ${{ github.event.pull_request.head.ref }} 

on:
  pull_request_review:
    types: [submitted]

jobs: 

  # Continuous integration workflow in 'ss-pipeline' repo
  continuous-integration:
    name: CI on SLFB
    if: ${{ github.event.review.state == 'approved' }}
    uses: dr3dr3/ss-pipeline/.github/workflows/continuous-integration.yml@main
    with: 
      feature: ${{ github.event.pull_request.head.ref }}
      target-ref: ${{ github.sha }}
      solution: 'revealmd'
    permissions:
      contents: write
      pull-requests: write
      pages: write
      id-token: write
    secrets: inherit